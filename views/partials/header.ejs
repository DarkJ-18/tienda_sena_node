<nav class="nav_styles navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="">
            <img src="/static/assets/canvas.png" alt="Logo" width="60" height="48" class="d-inline-block align-text-top">
        </a>
        <button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="/" class="nav-link active fw-medium text-white" aria-current="page" href="">Inicio</a>
                </li>
                <li class="nav-item">
                    <a href="/tienda" class="nav-link active fw-medium text-white" aria-current="page" href="">Tienda</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Categorías
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active fw-medium text-white" aria-current="page" href="">Sobre Nosotros</a>
                </li>
            </ul>
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
            <ul class="navbar-nav ms-auto">
                <% if (usuario) { %>
                    <% if (usuario.rol === 1) { %>
                        <li class="nav-item">
                            <a class="nav-link contact-button fw-medium text-white" href="/admin/productos">
                                <i class="bi bi-gear"></i> Administrador
                            </a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link contact-button fw-medium text-white iniciar-sesion-btn" href="/perfil">Mi Perfil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link contact-button fw-medium text-white" href="/logout">Cerrar sesión</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link contact-button fw-medium text-white" href="/registrarse">Regístrate</a>
                    </li>
                    <li class="nav-item iniciar-sesion-btn">
                        <a class="nav-link contact-button fw-medium text-white" href="/login">Inicie Sesión</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
<div id="notificaciones">
    <% if (typeof messages !== 'undefined' && messages.length > 0) { messages.forEach(function(message) { %>
        <div class="alert alert-<%= message.type && message.type !== 'error' ? message.type : 'danger' %> alert-dismissible fade show" role="alert">
            <%= message.text %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% }); } %>
</div>